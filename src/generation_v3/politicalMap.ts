import { Grid } from "./grid";
import { createNoise } from "../utils/createNoise";

export class PoliticalMap {
	noise = createNoise("PoliticalMap");

	constructor(private grid: Grid) {}

	initializeRegions(): void {
		//
	}

	initializeCountries(): void {
		//
	}
}
